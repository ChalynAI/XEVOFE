# NOTE: pnpm 11+ only reads AUTH and REGISTRY settings from this file.
# Behavior settings like node-linker / hoist-pattern / shamefully-hoist
# live in pnpm-workspace.yaml. See that file for the Windows long-path /
# CMake fixes.
